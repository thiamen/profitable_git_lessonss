# git init
# git add .
# git commit -m "Add personal comment(s)"
# git status
# git log (hit 'q' to exit)
# git checkout (copy/paste unique commit id)

Git Remote Commands
# git remote add origin (url)
# git remote -v
# git push origine master
# git pull origin master